apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "octopipe.fullname" . }}-test-connection"
  labels:
    {{- include "octopipe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "octopipe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
