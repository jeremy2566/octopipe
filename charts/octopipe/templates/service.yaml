apiVersion: v1
kind: Service
metadata:
  name: {{ include "octopipe.fullname" . }}
  labels:
    {{- include "octopipe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "octopipe.selectorLabels" . | nindent 4 }}
